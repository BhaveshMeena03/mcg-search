.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

.env

# Transcripts are large and re-fetchable from YouTube. The episode list is
# worth keeping in the repo; the full caption text is not.
data/episodes.json
# but DO commit the slim index: 85KB, and it is what makes a deploy
# work from a clean checkout. Rebuild with scripts/build_index_json.py
!data/episode_index.json
data/episodes.json.lock
data/episodes.json.tmp

.claude/
CLAUDE.md
AGENTS.md
.DS_Store

# Graded run outputs — regenerate with scripts/grade.py. They contain full
# answers, so they are large and change every run.
runs/
